#This is a camera and bar code detecting application and using this application the clicked images will be saved in the image gallery

dependencies-
npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar

npm install @react-native-community/slider --save

npm i @react-native-async-storage/async-storage -
we will store the image on the device asynchronously

npm i @expo/vector-icons

npx expo install expo-camera
set plugin in app.json
